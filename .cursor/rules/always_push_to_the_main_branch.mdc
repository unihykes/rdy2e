---
description: 本仓库内一切代码与文档的 Git 提交流程应直接在 main 上完成
alwaysApply: true
---

# 使用场景

在本仓库内对代码、配置或文档做任何修改、新增或更新，并在本地验证或确认可交付后，**始终在默认分支上提交并推送到主线**，而不是在单独功能分支上收尾再合并。

## 执行规范

- 在**默认分支 `main`** 上工作；若当前不在 `main`，先 `git checkout main`，必要时与远端同步（如 `git pull origin main`），再实施改动。
- 每轮可交付的改动都：**在 `main` 上** `git add` → `git commit -m "…"` → `git push origin main`（如需跟踪上游，可用 `git push -u origin main` 首次建立跟踪）。
- **不要**把日常改动只留在功能分支而不合并进 `main`；**不要**默认走「只开 PR、由他人合并到 main」而你自己不把变更推到 `origin/main`（若仓库允许你推 `main`，则你应直接推）。
- 提交信息简短、能说明本次变更；推送失败时先排查网络与权限，再重试。
